<template data-sly-template.article="${@ headline, body}">
  <sly data-sly-use.headlineTemplate="template-root-path/headline.htl"
       data-sly-call="${headlineTemplate.headline @ text=headline}"/>
  <p>${body}</p>
</template>