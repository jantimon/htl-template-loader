<template data-sly-template.article="${@ headline, body}">
  <sly data-sly-use.headlineTemplate="headline.htl"
       data-sly-call="${headlineTemplate.headlineBig @ text=headline}"/>
  <p>${body}</p>
</template>